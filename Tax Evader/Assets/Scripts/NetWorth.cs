using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NetWorth : MonoBehaviour
{

    int NetWorth;
    int Income;
    int projectedTax;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame

    //This function updates the net worth, income, and projected tax values.

    //Called when an asset is created
    public void UpdateWorth(int deltaWorth,int deltaIncome, int deltaTax)
    {
        
    }
    void Update()
    {
        
    }
}
