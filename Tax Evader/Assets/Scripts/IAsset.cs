using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IAsset 
{
    // Start is called before the first frame update
    //Add asset to data structure holding all of information
    void AddAsset();
}
